name: release
on:
  push:
    branch:
      - 'master'
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Publish Helm charts
        uses: stefanprodan/helm-gh-pages@master
        with:
          token: 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
          charts_dir: chartrepo

